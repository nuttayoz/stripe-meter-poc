{
  "name": "stripe-meter-poc",
  "private": true,
  "workspaces": [
    "application/*",
    "backend/*"
  ],
  "scripts": {
    "dev:web": "bun --cwd application/web run dev",
    "dev:api": "bun --cwd backend/api run start:dev",
    "dev": "echo \"Run 'bun run dev:web' and 'bun run dev:api' in separate terminals.\"",
    "infra:up": "docker compose -f devtool/docker-compose.yml up -d",
    "infra:down": "docker compose -f devtool/docker-compose.yml down",
    "infra:logs": "docker compose -f devtool/docker-compose.yml logs -f",
    "lint": "echo \"Lint will be wired after app scaffolding.\"",
    "test": "echo \"Tests will be wired after app scaffolding.\""
  }
}
